# 웹소설 집필 보조 & 편집기 통합 시스템 PRD (Product Requirements Document)

## 1. 프로젝트 개요
* **목표:** 웹소설 작가의 창작을 돕는 'AI 어시스턴트'와 본격적인 글쓰기를 위한 '전문 집필 모드(Scrivener 스타일)'를 하나의 프로그램으로 완벽히 통합.
* **타겟 환경:** Windows 로컬 데스크톱 앱 (향후 iPad/iOS 앱과 Supabase 기반 연동 예정).
* **기술 스택:** Python, PyQt6, SQLite(로컬 텍스트 캐시), Supabase(클라우드 동기화), LLM API(Gemini, Claude, GPT).

## 2. 코어 아키텍처 (QStackedWidget 기반 완벽 분리)
* **`main.py`:** 프로그램의 메인 윈도우. 좌측 상단에 모드 전환용 토글 버튼(QPushButton)을 배치하여, 클릭 시 `QStackedWidget`을 통해 두 모드를 스위칭함.
  * 상태 1: "📝 집필 모드로 전환" 버튼 표시 (현재 AI 어시스턴트 모드일 때)
  * 상태 2: "🤖 AI 어시스턴트로 전환" 버튼 표시 (현재 집필 모드일 때)
* **`mode_assistant.py`:** 기존 로직을 100% 유지. 무거운 API 통신(QThread)을 담당하는 로컬 전용 보조 툴.
* **`mode_writing.py`:** 작가가 실제로 원고와 설정을 작성하는 트리 형태의 전문 집필 에디터.

## 3. 데이터 및 폴더 관리 구조 (모드별 폴더 완벽 격리)
기존 어시스턴트 코드와의 충돌을 원천 차단하고 동기화를 단순화하기 위해, 집필 모드 전용 하위 폴더를 강제한다. 모든 폴더는 한글 파일명과 `utf-8` 인코딩을 엄격히 적용한다.
* `[작업 루트]/config.json` (전역 설정)
* `[작업 루트]/작품목록/[프로젝트명]/` (공통 프로젝트 최상위 폴더)
  * `어시스턴트/` (기존 AI 어시스턴트가 사용하는 로컬 폴더 영역)
  * **`집필모드/` (★신규: 집필 모드 전용 샌드박스 영역)**
    * `설정.json` (작품 개별 설정)
    * `메인/` 하위 폴더: `원고/`, `캐릭터/`, `설정집/`, `메모장/`, `플롯/` 등
    * `백업/` 하위 폴더: `자동저장/`, `전환직전/`

## 4. 모드별 데이터 동기화 및 브릿지 정책
* **AI 어시스턴트 모드 (Local Only):** * 기존 로직 유지. 클라우드 동기화 절대 개입 안 함.
  * **브릿지 기능:** 결과물 텍스트 박스 하단에 `[🚀 집필 모드로 보내기]` 버튼 배치. 클릭 시 텍스트를 집필 모드의 에디터로 전송하고 화면 자동 전환.
* **집필 모드 (Cloud Sync):** * **오직 `집필모드/` 하위 폴더의 데이터만** Supabase 서버와 양방향 동기화.
  * `Ctrl+S` -> 2초 디바운스 대기 -> Supabase 업로드 진행.
  * 프로그램 실행 및 작품 진입 시 `Editor Lock` (동시 편집 방지) 작동.

## 5. 집필 모드(`mode_writing.py`) 세부 UI
* **좌측 바인더 (`QTreeWidget`):** [원고, 캐릭터, 설정집, 메모장, 메인 스토리 틀, 흐름 정리, 복선, 장소, 휴지통] 고정 노드 제공.
* **우측 에디터 (`QSplitter`):** 2개의 `QTextEdit`을 좌우로 배치. [좌우 분할 모드 토글] 버튼 제공.
* **자동 권/화수 생성 로직:** [원고] 노드에서 `[권 추가]` 클릭 시, `집필모드/메인/원고/` 경로를 파악해 다음 권수 폴더(예: 3권)를 만들고, 25화 분량(051화.txt ~ 075화.txt) 빈 텍스트 생성 후 트리 갱신.

UI 제작 시 참조 사진 로컬 경로
![집필모드 UI 참고용 이미지](D:\안티그래비티\scratch\작가님 힘내세요\UI참조.jpg)