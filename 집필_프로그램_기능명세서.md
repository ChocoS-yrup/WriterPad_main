# 집필 프로그램 아키텍처 및 구현 명세서 (Multi-Platform 연동 참조용)

본 문서는 데스크톱(Windows) 기반으로 작성된 `mode_writing.py`와 `project_manager_writing.py`의 핵심 기능과 작동 방식을 매우 구체적으로 기술한 명세서입니다. 
추후 **아이패드 앱(iOS/iPadOS)** 등 다른 플랫폼으로 포팅하거나 연동할 때, 데이터 구조와 제약 조건(Constraints)을 동일하게 유지하기 위한 레퍼런스로 사용됩니다.

---

## 1. 파일 시스템 및 디렉토리 구조 (File System Mapping)

집필 프로그램의 모든 데이터는 로컬 파일 시스템을 기반으로 작동하며 별도의 데이터베이스를 사용하지 않습니다. 모든 경로는 프로젝트 폴더 내 `작품목록/[프로젝트명]/집필모드/` 를 루트(Root)로 가집니다.

### 1-1. 최상위 고정 카테고리 (Root Nodes)
앱의 좌측 바인더에 고정적으로 노출되는 9개의 최상위 카테고리(폴더)와 그에 대응하는 실제 디렉토리 경로는 다음과 같습니다.
* **디스플레이 명칭** ➔ **실제 상대 경로 (`집필모드/` 하위)**
  1. **`📚 원고`** ➔ `메인/원고/`
  2. **`👤 캐릭터`** ➔ `메인/캐릭터/`
  3. **`📖 설정집`** ➔ `메인/설정집/`
  4. **`📝 메모장`** ➔ `메인/메모장/`
  5. **`🗺️ 메인 스토리 틀`** ➔ `메인/플롯/`
  6. **`🌊 흐름 정리`** ➔ `메인/흐름정리/`
  7. **`🔍 복선`** ➔ `메인/복선/`
  8. **`📌 장소`** ➔ `메인/장소/`
  9. **`🗑️ 휴지통`** ➔ `휴지통/`

### 1-2. 파일 확장자 및 노출 방식
* 모든 텍스트 문서는 **`.txt`** 확장자를 가집니다. 파일 인코딩은 반드시 **UTF-8**을 사용합니다.
* UI 상에 파일 이름을 표시할 때는 뒤의 `.txt` 확장자를 숨기고 표시합니다. (예: `001화.txt` ➔ `001화`)
* 일반 폴더는 확장자가 없습니다.

---

## 2. 상태 저장 및 데이터 구조 (State Persistence)

앱의 상태 정보(순서 변경, 트리 펼침 상태)는 `집필모드/설정.json` 파일에 JSON 포맷으로 저장 및 관리됩니다.

### 2-1. `설정.json` 데이터 스키마
```json
{
    "tree_order": {
        "<root>": ["📚 원고", "👤 캐릭터", "📝 메모장", ...],
        "메인/캐릭터": ["주인공", "악당", "조력자"],
        "메인/메모장": ["아이디어.txt", "참고자료 폴더"]
    },
    "expanded_folders": [
        "메인/원고",
        "메인/캐릭터"
    ]
}
```

### 2-2. `tree_order` (사용자 정의 순서)
* **목적**: 사용자가 드래그 앤 드롭으로 재배치한 최상위 노드 및 하위 폴더/파일의 렌더링 순서를 기억합니다.
* **구조**: `키(Key)`는 부모 폴더의 상대 경로이며, `값(Value)`은 자식 항목들의 이름(basename)을 담은 문자열 배열(Array)입니다. 
* **최상위 노드 순서 (`"<root>"`)**: 최상위 카테고리 9개의 노출 순서를 배열로 저장합니다. (단, `📚 원고`는 무조건 최상단(Index 0)에 고정되도록 하드코딩 되어있어 배열 순서에 구애받지 않고 항상 1순위로 렌더링됩니다.)
* **정렬 로직(Sorting)**: 디렉토리를 로드(`os.listdir`)할 때, 배열에 이름이 존재하는 항목은 배열의 인덱스(Index) 순서대로 정렬합니다. 새로 생성되어 배열에 없는 항목은 기존 항목들의 뒤에 배치되며, 폴더가 텍스트 파일보다 먼저 오도록 정렬(`자연 정렬(Natural Sort)`)됩니다.

### 2-3. `expanded_folders` (트리 확장 상태)
* **목적**: 앱 재시작 시 사용자가 마지막으로 펼쳐두었던 트리 폴더들을 그대로 유지합니다.
* **구조**: 펼쳐진 폴더들의 상대 경로(`rel_path`)를 담은 배열입니다.

---

## 3. 핵심 비즈니스 로직 및 제약 조건 (Constraints & Rules)

다른 플랫폼을 개발할 때 반드시 동일하게 적용되어야 하는 애플리케이션 제약사항입니다.

### 3-1. '📚 원고' 폴더의 강제 구조화 (Strict Manuscript Structure)
원고 폴더(`메인/원고/`) 내부는 정해진 규격 외의 데이터 생성을 원천 차단합니다.
1. **권(Volume) 폴더 생성 제한**: 원고 폴더 바로 아래(Depth 1)에는 오직 **"N권"** 형태의 폴더만 생성할 수 있습니다. 텍스트 파일 생성은 불가능합니다.
2. **화수(Chapter) 문서 생성 제한**: 권 폴더 내부(Depth 2)에는 오직 **"M화"** 형태의 텍스트 파일만 생성할 수 있습니다. 하위 폴더 생성은 불가능합니다.

### 3-2. 접두사 보호 및 이름 변경 제약 (Prefix Locking)
1. **'권' 폴더 보호**: '📚 원고' 하위에 생성된 권 폴더('1권', '2권' 등)는 사용자가 이름을 수정(`Rename`)하는 것 자체가 완전히 불가능합니다.
2. **'화' 파일 접두사 고정**: 화수 파일(`001화.txt`)의 이름을 변경할 때, 접두사인 **"N화 " (띄어쓰기 포함)** 부분은 절대 지워지거나 수정될 수 없습니다. 
   * **정규식(Regex)**: `^(\d+화)` 패턴으로 매칭하여 접두사를 추출합니다.
   * UI 인풋 박스에서 백스페이스를 누르거나 드래그해서 지우려 시도해도, 글자 수가 접두사 길이보다 짧아지면 즉시 접두사를 원상 복구(`setText(self.prefix)`)시킵니다. (사용자는 'N화 뒤에 붙는 부제'만 수정 가능)

### 3-3. 드래그 앤 드롭 (Drag & Drop) 및 이동 규칙
사용자 경험(UX)과 파일 시스템 안전성을 보장하기 위한 Drag & Drop 물리 규칙입니다. 항목을 드롭하면 UI 상의 이동과 동시에 디스크의 파일도 물리적으로 이동(`shutil.move`)됩니다.
1. **원고 폴더 진출입 불가 (Manuscript Isolation)**: 
   * 외부 폴더(예: 메모장)의 파일을 '📚 원고' 내부로 드래그할 수 없습니다. (Drop 무시)
   * '📚 원고' 내부에 생성된 권 폴더('1권', '2권')나 화수 파일('1화.txt')은 외부로 빼낼 수 없을 뿐만 아니라, **내부에서의 순서 변경을 위한 드래그 앤 드롭조차도 원천적으로 금지**되어 있습니다. (이름에 따라 항상 자동 정렬 유지)
2. **최상위 노드 계층 보호 (Root Node Integrity)**: 
   * 9개의 최상위 노드(`📚 원고`, `👤 캐릭터` 등)는 다른 노드의 하위(자식)로 들어갈 수 없습니다. 오직 최상위 노드들 사이(위/아래)에서만 순서 이동이 가능합니다.
   * **원고 폴더 최상단 고정**: `📚 원고` 노드는 자체적으로 드래그가 불가능하며, 다른 어떤 최상위 노드도 `📚 원고` 위(Above)로 드래그해서 배치할 수 없도록 원천 차단되어 있습니다. 따라서 항상 트리 구조의 절대적인 최상단(Index 0)을 유지합니다.

### 3-4. 휴지통 관리 메커니즘 (Trash Bin System)
* 삭제 명령을 내린 파일이나 폴더는 영구 삭제되지 않고 `휴지통/` 폴더로 이동(`shutil.move`)됩니다.
* **이름 충돌 방지**: 휴지통으로 이동 시, 휴지통 내에 동일한 이름의 파일/폴더가 이미 존재할 경우, 이름 앞에 현재 시각의 타임스탬프(`int(time.time())_`)를 붙여 중복 덮어쓰기를 방지합니다.

### 3-5. 자동 저장 기능 (Auto-Save)
* 글 입력 도중 키보드 입력이 감지(`textChanged` 이벤트)되면 1초(1000ms)짜리 타이머가 재시작됩니다.
* 1초 동안 추가 입력이 없으면 타이머가 만료되며 현재 편집 중인 내용을 디스크(`write_text_file`)에 백그라운드로 자동 저장합니다.

### 3-6. 동기화 서버 접속 및 오프라인 예외 처리 (Sync & Offline Handling)
동시 편집 충돌을 방지하기 위한 Supabase 기반 서버 연동 시, 인터넷 연결 불안정 등 오프라인 상태에 대비한 예외 처리 로직이 탑재되어 있습니다.
1. **오프라인 모드 허용 (Offline Mode Fallback)**:
   * 파일 편집 권한(Lock) 획득을 위해 서버와 통신할 때 네트워크 장애가 발생하면, 편집을 강제로 막는 대신 내부적으로 **"오프라인 모드"**로 자동 전환하여 에디터 편집을 정상적으로 허용합니다. (인터넷 없이도 집필 연속성 보장)
2. **유휴 연결 자동 복구 (Keep-Alive Auto-Recovery)**:
   * 일정 시간 작업을 멈춰 서버 측 로드밸런서가 유휴 통신(Keep-Alive TCP 커넥션)을 강제로 끊어버린 상태에서 다시 타이핑/클릭을 시도할 경우, 파이썬 내부 HTTP 클라이언트(httpx)에서 "Server disconnected" 에러가 발생할 수 있습니다.
   * 이를 방지하기 위해 락(Lock) 획득 시 예외가 발생하면 **즉시 Supabase 클라이언트를 초기화하여 통신선을 복구하고 재시도(Retry)** 하는 자동 복구 메커니즘이 구현되어 있습니다.

---

## 4. UI 레이아웃 및 텍스트 에디터 (UI Layout & Editor)

### 4-1. 다중 화면 분할 (Multi-Splitter Layout)
* **기본 분할 (좌/우)**: 애플리케이션의 전체 레이아웃은 `QSplitter`를 사용하여 좌측의 '트리 바인더'와 우측의 '에디터 영역'으로 나뉩니다. 사용자는 마우스로 경계선을 드래그하여 너비를 자유롭게 조절할 수 있습니다.
* **듀얼 에디터 분할 (우측 영역 내부)**: 우측 에디터 영역 자체도 수평(가로 방향)으로 2분할이 가능합니다. 한쪽에는 설정집이나 캐릭터 시트를 열어두고, 다른 한쪽에서는 원고를 작성하는 등 2개의 텍스트 문서를 나란히 띄워놓고 작업할 수 있는 듀얼 뷰(Dual View) 환경을 제공합니다.
* **마지막 활성화 탭 자동 복구 (Remember Active Tab)**: 앱을 종료하기 직전에 마우스 커서가 머물러 있던(마지막 활성화 상태였던) 탭(좌/우)을 `설정.json`에 기록합니다. 앱을 다시 실행할 때 좌우 분리 모드가 켜져 있다면, 이전에 열려 있던 파일들과 함께 포커스를 마지막에 작업하던 위치로 정확하게 복원합니다.

### 4-2. 스마트 텍스트 에디터 (SmartTextEdit)
우측 편집 영역에 사용되는 핵심 컴포넌트인 `SmartTextEdit`는 단순 텍스트 입력 이상의 다양한 서식 및 유틸리티를 제공합니다.

1. **상단 서식 툴바 (Formatting Toolbar)**:
   * **텍스트 스타일링**: 굵게(Bold), 기울임(Italic), 밑줄(Underline), 취소선(Strikethrough) 토글 기능이 제공됩니다.
   * **글꼴 및 크기 제어**: 시스템 폰트를 선택할 수 있는 콤보박스와 텍스트 포인트 크기 조절 기능이 있습니다.
   * **줄 간격 조절**: 1.0부터 3.0 배수까지 줄 간격(Line Height) 조절이 가능합니다.
   * **색상 지정**: 글자색(Text Color)과 글자 배경색(Highlight Color)을 컬러 팔레트를 띄워 자유롭게 변경할 수 있습니다.
   * **문단 스타일 적용**: '제목 1, 2, 3' 및 '본문' 등 사전에 정의된 텍스트 크기와 볼드 처리를 원클릭 콤보박스로 적용할 수 있습니다.

2. **실시간 글자 수 카운팅 (Character Count)**:
   * 에디터 하단의 상태표시줄 영역에 현재 문서의 총 글자 수(공백 포함)가 실시간으로 계측되어 노출됩니다.

3. **포커스 활성화 강조 (Active Focus Highlighting)**:
   * 듀얼 에디터 분할 모드에서 사용자가 현재 작업 중인(포커스가 있는) 에디터 창을 시각적으로 뚜렷하게 구분해줍니다.
   * 활성화된 에디터를 감싸는 패널에는 밝은 파란색(`cyan` 계열)의 둥근 테두리 및 상태표시 텍스트 강조가 적용되며, 비활성 에디터는 회색조로 렌더링됩니다.
   * 하단 상태창과 텍스트 입력창이 단 1픽셀의 틈새나 어긋남 없이 밀착되도록 세밀하게 CSS 박스 모델 및 곡률(`border-radius`)이 설계되어 있습니다.

4. **타자기 스크롤 모드 (Typewriter Scroll)**:
   * 장시간 집필 시 작업자의 목 피로도를 줄여주기 위해, 텍스트 커서가 에디터 화면의 수직 중앙(Center-Y)을 넘어가는 순간부터 자동으로 스크롤을 내려 커서를 시야 중앙에 고정시키는 타자기 스크롤 기능이 탑재되어 있습니다.
   * 사용자 설정 탭에서 온오프 토글이 가능하며, 해당 값(`tw_writing`)은 `global_config`에 저장되어 재시작 시에도 유지됩니다.
   * 마지막 줄 작성 시 하단에 충분한 시각적 여백 공간을 제공하여 스크롤 동작이 자연스럽게 이루어지게 돕습니다.

5. **강력한 검색 기능 (Editor & Global Search)**:
   * **현재 문서 검색 (Ctrl + F)**: 
     * 에디터 내에서 단축키를 입력하면 자체 검색 바(Search Bar)가 나타나며, 현재 열려 있는 문서 내부의 텍스트를 즉시 탐색할 수 있습니다.
     * 이전 찾기, 다음 찾기 기능이 지원되어 문서 내 특정 단어나 문장의 위치로 빠르게 이동 가능합니다.
    * **프로젝트 전체 검색 (Ctrl + Shift + F / 전역 검색 버튼)**:
      * 현재 선택된 프로젝트 내의 **모든 텍스트 파일**(`*.txt`)을 대상으로 통합 검색을 수행할 수 있는 `GlobalSearchDialog` 창이 제공됩니다.
      * 검색된 결과는 파일별로 그룹화되어 리스트 형태로 출력되며, 해당 항목을 클릭하면 즉시 그 파일이 에디터에 열리고 대상 텍스트가 하이라이트(Highlight) 처리되어 사용자의 편의성을 극대화합니다.

6. **지능형 빈 문서 안내 문구 (Smart Placeholder)**:
   * 내용이 없는 빈 파일에 진입했을 때 "텍스트 입력" 이라는 시각적 안내 문구(Placeholder)를 띄워줍니다.
   * 이 문구는 `QTextEdit`의 문서 여백(Margin) 계산 수치를 정확하게 따르기 때문에 글을 작성할 실제 위치에 완벽하게 정렬됩니다.
   * **IME(한글 입력기) 조합 인식**: 글자를 완성하지 않고 초성 하나(예: 'ㄱ', 'ㅇ')만 쳐서 글자 조합(Preedit) 상태일 때도 프로그램이 이를 똑똑하게 감지(`inputMethodEvent` 오버라이딩)하여 안내 문구가 한글과 겹쳐 보이는 현상을 사전에 방지하고 즉시 숨김 처리합니다.

7. **에디터 자동 포커스 (Auto-Focus on Load)**:
   * 좌측 파일 트리(바인더)에서 문서를 클릭해 불러오는 즉시 프로그램이 키보드 입력 포커스(`setFocus`)를 에디터 창으로 강제 전환합니다. 
   * 이로 인해 사용자는 마우스 클릭 후 곧바로 키보드 타자를 이어나갈 수 있어 문서 전환 시 동선과 피로를 단축합니다.
