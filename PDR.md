웹소설 집필 보조 AI 어시스턴트 프로그램 최종 기획서
1. 프로젝트 개요
목적: 웹소설 작가의 리소스가 크게 소모되는 요약, 초안 작성, 내용 평가 단계를 AI로 자동화하여 오직 핵심 창작(플롯 구상, 최종 집필)에만 집중할 수 있는 환경 제공.

타겟 환경: Windows 10 기반 로컬 데스크톱 애플리케이션

핵심 가치: 안정성(데이터 무결성), 가벼운 구동(UI 응답성), 유연한 확장성(멀티 모델 지원)

2. 기술 스택 및 구동 환경
프론트엔드/백엔드: Python, PyQt6 (디자인보다 처리 속도와 UI 멈춤 방지에 최우선 집중)

비동기 처리 (필수): API 통신 및 무거운 검색 작업 시 메인 프로그램이 절대 멈추지 않도록 QThread를 완벽하게 적용. 로딩 중에는 인디케이터 시각화.

API 연동 (모듈화 구조): google-generativeai, anthropic, openai 라이브러리를 모두 포함하여, 설정 파일에 따라 API 엔드포인트를 유연하게 전환할 수 있는 구조 설계.

로컬 하드웨어 가속: 사용자의 RTX 4060 (CUDA) 활용. 무거운 LLM 텍스트 생성은 클라우드 API에 맡기고, 로컬 GPU는 '전체 텍스트 초고속 검색 기능(임베딩 기반 또는 GPU 가속 키워드 검색)'에만 제한적으로 사용하여 시스템 리소스를 확보.

3. 핵심 파이프라인 (작업 흐름)
Phase 1: 컨텍스트 준비 (25화 분기점 스마트 로직)
초반부의 촘촘한 빌드업 보존과 중후반부의 토큰 비용/망각 현상을 동시에 해결하는 로직입니다.

현재 작업 화수(N)가 25화 이하일 때: 요약 단계를 거치지 않고, 1화부터 N-1화까지의 연재본 원문 전체를 취합하여 초안 모델의 컨텍스트로 직행.

현재 작업 화수(N)가 26화 이상일 때: [1화 ~ N-6화까지의 누적 요약본] + [N-5화 ~ N-1화까지의 원문 전체]를 취합하여 전달. (요약 데이터는 설정된 요약 모델을 통해 정해진 토큰 분량 내에서 지속적으로 누적 및 압축 병합)

Phase 2: 초안 단계
Phase 1에서 취합된 텍스트(Context)와 사용자가 직접 작성한 대사/상황 플롯(Prompt)을 설정된 초안 모델에 병합 전송.

기존 스토리의 흐름, 캐릭터의 성격, 문체의 일관성이 완벽하게 유지된 초안 텍스트를 생성하여 반환.

Phase 3: 평가 단계
사용자가 스크리브너 등에서 최종 완성한 원문을 프로그램에 입력.

설정된 평가 모델이 현재 화수의 재미 요소, 단점을 피드백하고 "더 도파민이 터지는 전개를 위해 발생해야 할 다음 사건 아이디어 3가지"를 제시.

4. 세부 기능 및 UI/UX 요구사항
① 다중 AI 모델 선택 기능 (Model Selector)
향후 상위 모델 등장에 대비해 [설정 탭]에서 각 단계별 AI 모델을 드롭다운으로 선택 가능.

지원 모델 라인업: Gemini 3.1 Pro, Gemini 3.1 Pro (확장모드), Claude Opus 4.8, Claude Opus 4.8 높음, GPT-5.5

기본값 고정: * 요약 단계 = Gemini 3.1 Pro

초안 단계 = Claude Opus 4.8

평가 단계 = Gemini 3.1 Pro

② 스마트 콤보박스 (안전한 화수 이동)
다이얼 방식: 001~9999화까지 마우스 휠로 백, 십, 일의 자리를 개별적으로 굴려 맞출 수 있는 커스텀 위젯.

오작동 방지: 휠로 숫자를 조절한 후에는 반드시 옆에 있는 [이동(Go)] 버튼을 클릭해야만 해당 화수 데이터가 로드됨.

즉각 반응: 단, 콤보박스 양옆의 < (이전 화), > (다음 화) 꺾쇠 버튼을 누를 경우에는 버튼 클릭 즉시 화수가 이동하고 데이터가 로드됨.

③ 지식 베이스 (NotebookLM 스타일 설정집 사이드바)
메인 UI 우측에 토글(열기/닫기) 가능한 사이드바(Dock Widget) 패널 배치.

온디맨드 추출: [설정집 업데이트] 버튼 클릭 시 백그라운드에서 전체 텍스트(요약+최근 원문)를 API로 보내 등장인물 리스트, 성격, 타 인물과의 관계, 주요 떡밥을 마크다운 표/JSON 형태로 추출하여 갱신.

로컬 검색 연동: 사이드바에 추출된 특정 캐릭터 이름이나 고유명사를 클릭/더블클릭하면, 로컬 GPU 검색 기능이 즉시 작동하여 해당 캐릭터가 등장한 원문 위치(화수)를 초고속으로 리스트업.

④ 편의성 및 모니터링 패널
실시간 글자 수 카운팅: 사용자가 글을 입력하는 텍스트 박스와 AI가 반환하는 결과물 박스 하단에 '현재 글자 수 (공백 포함 / 공백 제외)'를 실시간으로 표시하여, 웹소설 1화 표준 분량을 직관적으로 체크할 수 있도록 지원.

비용 모니터링: 하단 상태 표시줄(Status Bar)에 현재 작업 화수, 진행 상태, 그리고 이번 작업에 소모될 '예상 토큰 수 및 예상 비용(현재 입력된 글자 수 및 선택된 모델의 단가 기반)'을 실시간 표시.

결과물 처리: 각 결과물 텍스트 박스 우측 상단에 [📋 복사] 버튼 배치.

자동 다음 화 전환: 평가 단계를 마치고 [완성 및 다음 화로] 버튼을 누르면 파일이 자동 저장 및 텍스트 정리 후 다음 화수로 화면 자동 갱신.

5. 데이터 무결성 및 백업 시스템
작가의 텍스트 유실을 원천 차단하기 위해 철저한 분산 및 백업 관리를 적용합니다.

단계 간 전환 시 스냅샷 보존 (Pre-Transition Backup):

요약 ➔ 초안, 초안 ➔ 평가, 평가 ➔ 다음 화 요약 등 어떤 단계에서든 다른 단계로 데이터를 주고받거나 화면/상태가 전환되는 트리거(버튼 클릭)가 발생할 때마다, 기존 텍스트 박스에 쓰여 있던 내용이 덮어씌워져 날아가는 것을 방지하기 위해 [작업폴더]/backup/pre_transition/ 경로에 화면 전환 직전의 모든 텍스트 상태를 무조건 스냅샷 형태로 선행 백업할 것.

3중 자동 백업:

오토 세이브: 텍스트가 변경되거나 API 응답이 완료될 때마다 로컬 backup/ 경로에 041화_초안_20260630_1320.txt와 같이 타임스탬프를 달아 무한히 숨김 저장.

버전 관리: 동일 화수에서 여러 번 결과를 생성해도 덮어쓰지 않고 _v1, _v2 형태로 분리 저장.

자동 분류: 최종 결과물은 Summary, Drafts, Completed 폴더로 나뉘어 .txt로 영구 저장.

설정값 분리 (config.json):

각 단계별 System Prompt와 선택된 AI 모델 정보 등은 코드에 하드코딩하지 않고 외부 JSON 파일로 관리.

프로그램 내 [설정 관리] 탭에서 앱을 끄지 않고도 텍스트 및 모델 변경을 실시간으로 수정 및 반영 가능.

6. 프로그램 안정성 및 시스템 제어 (System Control)
데스크톱 앱 환경에서 발생할 수 있는 데이터 꼬임, 파일 손상 및 메모리 누수를 방지하기 위해 아래의 시스템 제어 로직을 반드시 구현할 것.

6-1. 시스템 트레이(작업 표시줄) 최소화 옵션:

[설정 탭]에 종료(X) 버튼 클릭 시 트레이로 최소화 기능을 On/Off 할 수 있는 토글 추가.

On 상태일 때: 우측 상단의 X 버튼을 누르면 프로그램이 종료되지 않고 시스템 트레이(작업 표시줄 우측 하단 아이콘)로 숨김 처리되어 백그라운드 대기.

Off 상태일 때: X 버튼을 누르면 즉시 프로그램 완전 종료 프로세스 실행.

6-2. 완벽한 프로세스 종료 (유령 프로세스 방지):

트레이 최소화 설정이 Off인 상태에서 X를 누르거나, 시스템 트레이 아이콘을 우클릭하여 [종료]를 선택했을 경우, 앱이 메모리에 남지 않도록 완벽하게 종료되어야 함.

기술적 요구사항: 종료 이벤트 발생 시 백그라운드에서 대기 중인 리소스를 안전하게 해제하고, QApplication.quit() 및 sys.exit()를 호출하여 작업 관리자 프로세스 목록에서 즉각적으로 완전히 사라지도록 구현할 것.

6-3. 단일 실행 보장 (Single Instance Application):

프로그램 중복 실행으로 인한 자동 백업 파일 덮어쓰기나 텍스트 꼬임을 방지하기 위해, 하나의 PC에서 단 하나의 인스턴스만 실행되도록 제한.

기술적 요구사항: QSharedMemory 또는 Mutex 락을 사용하여 프로그램 실행 시 이미 구동 중인 프로세스가 있는지 체크. 만약 프로그램이 켜져 있는 상태에서 아이콘을 또 클릭하여 실행할 경우, 새 창을 띄우지 않고 기존에 실행되어 있던 프로그램 창을 최상단으로 끌어올려(Focus) 보여줄 것.

6-4. 작업 중 강제 종료 방지 (Safe Exit 로직):

AI API가 통신 중(요약, 초안, 평가 등)이거나 로컬 백업 파일이 저장되는 도중에 사용자가 X 버튼이나 종료 명령을 내릴 경우, 데이터 손실을 막기 위해 종료를 차단.

기술적 요구사항: 메인 윈도우의 closeEvent를 오버라이딩하여 현재 실행 중인 QThread(작업 스레드)가 있는지 상태를 검사할 것. 작업 중일 경우 "사용 중입니다. (현재 작업이 완료된 후 종료할 수 있습니다)"라는 경고 팝업(QMessageBox)을 띄우고 종료 이벤트를 무시(Ignore)하여 텍스트 무결성을 100% 보장할 것.