# WriterPad does not load or package cloud client settings from .env files.
# Public release settings live in release_cloud_config.json, which permits only
# the Supabase URL and publishable key. User sessions remain in Windows
# Credential Manager; email, password, JWT and refresh tokens are never release
# configuration fields.
